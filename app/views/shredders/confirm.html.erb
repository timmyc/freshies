<div class="sixteen columns">
  <% if @shredder.confirmed -%>
  <h3>Clearance Clarence</h3>
  <p>Your account is already confirmed.  You will be receiving all the alerts via text now.  Awesome!</p>
  <% else -%>
  <h3>Almost Done!</h3>
  <p>You should be getting a text right about.... now.  Fill out the confirmation code in the form below, and you are all signed up!</p>
  <%= form_for @shredder, :url => :do_confirm do |f| %>
    <%= f.label :mobile, 'Mobile:' %>
    <%= f.text_field :mobile %>
    <%= f.label :mobile, 'Confirmation Code:' %>
    <%= f.text_field :confirmation_code, { :value => '' } %>
    <%= f.submit 'Confirm', {:id=>'signup'} %>
  <% end %>
  <h4>Ehh What Code?</h4>
  <p>If you didn't get a text message, click the button below to have it resent to <%= @shredder.mobile %>:</p>
  <a href="/confirm?resend=1" class="button">Resend Confirmation Code</a>
  <% end -%>
</div>
